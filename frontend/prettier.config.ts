import { type Config } from 'prettier';

const config: Config = {
    tabWidth: 4,
    trailingComma: 'es5',
    semi: true,
    singleQuote: true,
    arrowParens: 'always',
    htmlWhitespaceSensitivity: 'strict',
};

export default config;
